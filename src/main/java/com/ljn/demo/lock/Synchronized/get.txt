1、notify并不会释放锁，只是唤醒其他线程，执行完同步代码块才会释放锁
2、获取不到锁对象的线程和调用wait的线程不是放在一起的
一个monitor锁会对应两个队列，一个队列是存放获取不到该锁对象的线程，即阻塞队列，另一个队列存放调用该锁对象的wait方法的线程，即等待队列，调用notify
只是会将线程从等待队列移到阻塞队列，当释放锁后（同步代码块执行完或者调用wait方法），其他线程才会去获取锁